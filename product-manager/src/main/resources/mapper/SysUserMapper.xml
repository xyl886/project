<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.love.product.mapper.SysUserMapper">


    <select id="selectNameAndPassword" resultType="com.love.product.entity.UserInfo">
        select a.id,a.username,a.role_id,a.status,b.nickname,b.avatar from b_user a left join (select id,nickname,avatar from user_auth) b on a.user_auth_id=b.id
        where username=#{username} and password=#{password}
    </select>
</mapper>
